class Solution:
    def addBinary(self, a: str, b: str) -> str:
        # int(string, 2) converts binary to decimal
        # bin() converts decimal back to binary (prefixed with '0b')
        # [2:] slices the string to remove the '0b'
        return bin(int(a, 2) + int(b, 2))[2:]
